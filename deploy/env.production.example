# Server-side RPC (never sent to browsers; /api/rpc proxies wallet calls).
SOLANA_RPC=
# Public origin, used for hosted token metadata links (written on chain, permanent).
PUBLIC_BASE_URL=https://stockcurve.pewcake.fun
